# A set is a collection in which each item must be unique. 

# We can build a set directly with braces, just like we build a list or a dictionary. The only difference is we don't include any key-value pairs as we do in a dictionary. We just list the items in the set, separated by commas.
languages = {'python', 'ruby', 'python', 'c'}
print(languages)